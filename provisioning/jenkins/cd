pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                echo 'deploy'
            }
        }
    }

    post {
        always {
            echo "post"
            cleanWs()
        }
    }
}
